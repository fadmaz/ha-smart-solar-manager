recompute_plan:
  name: Recompute plan
  description: Recalculate smart solar recommendation immediately.
  fields:
    entry_id:
      required: false
      selector:
        text:

execute_plan:
  name: Execute plan
  description: Execute current recommended actions if allowed by safety options.
  fields:
    entry_id:
      required: false
      selector:
        text:
    force:
      required: false
      default: false
      selector:
        boolean:
    dry_run:
      required: false
      selector:
        boolean:
